
# Frontend - Mentor-Based Student Performance System

## Setup

1. Install dependencies: `npm install`
2. Run development server: `npm run dev`

## Structure

- `src/components`: Reusable UI components (Navbar, Guards)
- `src/context`: Auth state management
- `src/pages`: Page components (Dashboards, Login)
- `src/services`: API handling (Axios)
